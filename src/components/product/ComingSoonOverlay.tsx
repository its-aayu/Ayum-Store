export function ComingSoonOverlay() {
  return (
    <div className="absolute inset-0 flex items-center justify-center bg-ink/50">
      <span className="rounded-pill bg-surface px-4 py-1.5 text-xs font-semibold uppercase tracking-wide text-ink">
        Coming Soon
      </span>
    </div>
  );
}
